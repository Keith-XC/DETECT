name: detect_env
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  # ---- Python ----
  - python=3.9.19
  - pip=24.0

  # ---- Core numeric ----
  - numpy=1.22.4
  - pandas=2.2.3
  - scikit-learn=1.6.1

  # ---- Visualization (optional but common) ----
  - matplotlib=3.4.2

  # ---- PyTorch ----
  - pytorch=2.4.0
  - torchvision=0.19.0
  - cudatoolkit=11.1.74

  # ---- Utilities ----
  - tqdm=4.67.1
  - regex=2024.11.6

  - pip:
      - zennit==0.5.1
      - pillow==11.2.1

      # ---- NEW: config & API ----
      - dotenv==0.9.9
      - openai==2.2.0